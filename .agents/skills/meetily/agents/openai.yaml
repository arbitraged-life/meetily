interface:
  display_name: "Meetily"
  short_description: "Repo-specific patterns and workflows for meetily"
  default_prompt: "Use the meetily repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true